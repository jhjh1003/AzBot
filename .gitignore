# 빌드 산출물
bin/
obj/
.vs/

# 로컬 전용 설정 (토큰/키가 들어갈 수 있는 파일 — 절대 커밋하지 않음)
.env
.env.*
appsettings.Local.json
appsettings.Development.json
*.user

# 로컬 SQLite 데이터
*.db
*.db-shm
*.db-wal

# Claude Code 로컬 런타임 상태 (세션 락 파일 등 — 커밋 대상 아님)
.claude/

# 클라우드 배포용 SSH 개인키 (절대 커밋 금지 — 노출되면 서버에 누구나 접속 가능해짐)
*.key
*.pem
