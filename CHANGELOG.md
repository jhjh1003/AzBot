# 변경 이력 (Changelog)

LoL Helper Bot의 기능 추가/변경 내역을 버전별로 기록합니다.

**새 항목을 추가하는 규칙**: 기능을 추가하거나 기존 동작을 바꿀 때마다, 이 파일 맨 위(가장 최근 버전 위)에 새 `## [x.y.z] - YYYY-MM-DD` 항목을 만들고 `추가`/`변경`/`수정`/`제거` 중 해당하는 항목 아래에 한 줄로 적어주세요. 날짜는 KST 기준입니다. 버전 번호는 [Semantic Versioning](https://semver.org/lang/ko/)을 따릅니다 — 기존 명령어의 동작이 바뀌면 minor(중간 자리), 새 명령어/기능이 추가되면 minor, 버그 수정만 있으면 patch(마지막 자리)를 올립니다.

**아직 구현 안 한 아이디어/2단계 업그레이드는 여기 말고 [AfterUpgrade.md](AfterUpgrade.md)에서 관리합니다.** 이 파일은 "이미 배포된 것"만 남겨두는 곳입니다.

## [0.13.0] - 2026-08-22

### 추가

- **`/승률순위`에 월간 순위 추가(사용자 요청).** 기존 전체 기간 순위 아래에 "📅 {연월} 승률 순위" 필드가 새로 붙음 — `연월` 옵션을 생략하면 이번 달(KST 기준), `2026-08` 형식으로 다른 달도 조회 가능(`/명예의전당`과 동일한 파싱 방식). 전체 순위와 마찬가지로 최소 표본 기준을 적용하고 부족하면 그 달 전체 데이터로 폴백. `MatchRepository.GetMemberWinRatesAsync`에 `fromUtc`/`toUtc` 옵션 파라미터 추가.

## [0.12.0] - 2026-08-22

### 추가

- **`/atoz 전적수집`이 계정별 "마지막 수집 시점"을 기억해서 그 이후 매치만 다시 훑도록 변경(사용자 요청).** 지금까지는 `최근경기수`를 얼마로 주든 매번 2026-08-01부터 전체 구간을 다시 요청했는데, 이제 계정마다 지난 수집이 끝난 시점을 `member_collection_checkpoints` 테이블에 기록해두고 다음 수집부턴 그 시점 이후만 Riot에 물어봅니다. **새로 등록한 멤버·부캐는 체크포인트가 없어서 자동으로 처음부터(2026-08-01~) 전체 스캔**되므로 별도 처리가 필요 없습니다. 안전장치: `recentCount` 한도에 걸려 그 구간을 다 못 받았으면(딥 백필 등) 체크포인트를 앞당기지 않아서 다음 수집도 같은 시점부터 다시 훑고, Riot API 반영 지연을 감안해 체크포인트에 15분 여유를 둡니다(막 끝난 경기가 다음 수집에서 영구 누락되는 것 방지). `RiotApiClient.GetMatchIdsAsync`/`ClanStatsModule.GetAllMatchIdsAsync`가 "이 구간을 빠짐없이 다 받았는지(Complete)"를 같이 반환하도록 확장.

## [0.11.1] - 2026-08-22

### 추가

- **`/atoz 전적수집` 중복 실행 방지 가드 추가(사용자 질문 계기 — "수집 중에 다른 명령 쓰면 뻗지 않냐").** 확인 결과 `/티어픽`·`/내전적` 같은 조회 명령은 짧은 SQLite 쿼리 하나씩이라 전적수집과 동시에 실행돼도 안전함(Microsoft.Data.Sqlite 기본 busy timeout 30초 안에서 자동 재시도됨) — 그래서 진짜 위험한 "`/atoz 전적수집`을 실수로 두 번 동시에 돌리는 경우"(Riot API 요청 2배, `checked_matches` 캐시 경합)만 막도록 `Interlocked` 기반 정적 가드 추가. 이미 진행 중일 때 다시 실행하면 "⏳ 이미 진행 중" 안내만 뜨고 새로 시작하지 않음.

## [0.11.0] - 2026-08-22

### 변경

- **`/atoz 전적수집`이 2026-08-01 이전 경기는 저장하지 않도록 변경(사용자 요청).** `최근경기수`를 300처럼 크게 줘도 그 이전 날짜 경기는 걸러집니다 — 과거 API 키 교체 이력 등으로 그 이전 데이터에 정합성 문제가 있었던 적이 있어(0.3.0 "데이터" 항목 참고), 앞으로의 수집 범위를 안전하게 제한. 걸러진 매치도 "확인 완료(저장 안 됨)"로 캐싱해서 다음 수집 때 다시 조회하지 않습니다. 기준일은 `ClanConstants.MatchCollectionCutoffUtc`. 이미 저장된 그 이전 데이터는 그대로 유지됨(삭제 안 함).
  - **API 호출 낭비 방지(사용자 질문 계기).** 처음엔 매치 상세를 다 받아온 뒤에 날짜로 거르는 방식만 있었는데, "`최근경기수:200`을 줘도 Riot한테 200개를 다 물어보는 거 아니냐"는 질문을 받고 확인해보니 실제로 그랬음 — Riot의 매치ID 목록 API가 지원하는 `startTime` 파라미터(그 시각 이전 매치는 서버가 애초에 목록에서 제외하고 응답)를 `RiotApiClient.GetMatchIdsAsync`에 추가하고 `/atoz 전적수집`에서 컷오프를 여기에도 넘기도록 수정. 이제 오래된 매치는 ID 목록 단계에서부터 빠져서 상세 조회(API 호출)가 애초에 발생하지 않음.

## [0.10.0] - 2026-08-22

### 추가

- `/atoz 내전적`에 "최근 N경기 서머리"(최근 최대 10경기 몇승몇패·승률, 모스트 픽 챔프 3개, 평균 AZ기여도 순위)와 "최근 전적"(최근 3경기 각각 승패·챔피언·KDA·AZ기여도 순위) 섹션 추가(사용자 요청) — "총 승률" 바로 아래에 표시. 월 필터를 걸어도 이 두 섹션은 항상 진짜 최신 경기 기준(필터와 무관)이라 헷갈리지 않게 필터가 걸려있을 땐 제목에 표시. `MatchRepository.GetRecentMemberMatchesAsync` 추가, `/아재전적`과 기여도 순위 계산 로직(`ComputeContributionRanks`)을 공유하도록 리팩터링.

## [0.9.0] - 2026-08-22

### 추가

- `/atoz 전적재배정` 추가 — "부캐 소유자 충돌"(같은 경기에 본인+빌린 사람이 동시에 있어 저장 자체가 씹히는 경우, `/atoz 부캐충돌목록`·`/atoz 부캐충돌해결`이 처리)과 달리, 저장은 정상적으로 됐지만 **그 경기만 다른 사람이 부캐를 빌려서 한 경우**를 위한 운영자 전용 명령. 매치ID + 기존멤버 + 새멤버를 주면 Riot API 재조회 없이 `match_participations`·`match_contribution_v4`의 소유자만 그대로 옮김(대상 멤버가 이미 같은 경기 기록을 갖고 있으면 진짜 부캐 충돌로 보고 거부). `MatchRepository.ReassignParticipationOwnerAsync` 추가.
- `/atoz 아재전적` 각 경기 카드 맨 아래에 매치ID를 작게 표시 — `/atoz 전적재배정`에 바로 붙여넣을 수 있게.
- `.github/workflows/docker-publish.yml` 추가 — `main`에 push하면 GitHub Actions가 Docker 이미지를 빌드해서 `ghcr.io/jhjh1003/azbot`에 자동으로 푸시. 오라클 Always Free VM(1GB RAM)에서 `docker build`가 메모리 부족으로 죽던 문제 해결(빌드를 VM 밖에서 하도록 변경). `DEPLOY.md`를 이 흐름에 맞춰 갱신.

## [0.8.1] - 2026-08-21

### 추가

- 부캐 소유자 충돌 진단/일괄 처리용 콘솔 도구 2종 추가: `Tools/ConflictInspectExperiment.cs`(`dotnet run -- inspect-conflict <matchId>`, 읽기 전용 — 저장된 롤아이디를 코드포인트 단위로 찍어서 공백/특수문자 문제 확인), `Tools/OwnerConflictResolveExperiment.cs`(`dotnet run -- resolve-conflict <롤아이디> <멤버닉네임> <매치ID...>` — `/atoz 부캐충돌해결`과 동일 로직을 매치 여러 건에 한 번에 적용). `ClanStatsModule.BuildParticipationStats`를 `internal`로 바꿔 재사용.
- 이 도구로 KR_8349424247(Thresh)·KR_8349368889(Maokai) 2건의 "라 칸#POOH" 충돌을 ggyadong 멤버로 해결 완료(운영 DB 직접 반영).

## [0.8.0] - 2026-08-21

### 추가

- `/내전적`에 `월` 옵션 추가 — 생략하면 지금처럼 전체 기간 통합, `8`/`8~8`(8월만)/`6~8`(6~8월) 형식으로 넣으면 이번 해(KST 기준) 해당 월(들)만 필터링해서 보여줌. `MatchRepository`의 관련 3개 쿼리(라인별/챔피언별/라인+챔피언별 집계)에 옵션 날짜 범위 파라미터 추가.

## [0.7.3] - 2026-08-21

### 변경

- `/내전적`의 라인별 모스트 챔피언 TOP3 정렬 기준을 승률순 → **판수(많이 한 순)**으로 변경(사용자 요청 — 표본 1판짜리 100% 챔프가 실제 모스트보다 위에 뜨던 문제).

## [0.7.2] - 2026-08-21

### 변경

- 기여도 v4.0.0 후반(15분 이후) 블렌드 비율 조정: 맞라인 상대 비교 30% → **40%**, 팀 내부 비교 70% → **60%**(사용자 요청). 2026-08 매치 49건 재백필 완료.

## [0.7.1] - 2026-08-21

### 변경

- **기여도 v4.0.0의 15분 이후(후반) 계산 방식 정교화.** 라인전(15분 이전)은 맞라인 상대 1:1 비교를 그대로 유지하고, **15분 이후는 "맞라인 상대 비교"(30%) + "팀 내부(나 제외 팀원 4명 평균) 비교"(70%)를 섞도록 변경**(사용자 요청 — 후반은 팀파이트/오브젝트 위주라 맞라인 상대와의 비교보다 "지금 이 팀에서 누가 잘하고 있나"가 더 중요하다는 판단). 골드/CS/경험치/딜량/받은피해량/CC/와드/오브젝트/힐+보호막/KDA 전부 이 블렌드를 적용. `Services/ContributionScoreCalculatorV4.cs`.
- **2026-08 매치 49건 재백필** — 새 블렌드 로직으로 `match_contribution_v4` 점수 갱신.
- `Tools/ContributionScoreV4Experiment.cs`를 `ContributionScoreCalculatorV4` 서비스를 호출하는 방식으로 리팩터링(계산 로직 중복 제거 — 서비스만 고치면 진단 도구도 자동으로 최신 상태 유지). 대신 포지션별 평균 Advantage 진단 출력은 이번에 빠짐(필요하면 재추가 가능).

## [0.7.0] - 2026-08-21

### 변경

- **기여도 점수 v4.0.0을 `/아재전적`·`/명예의전당`에 정식 적용.** "15분 라인전/후반 분리 + 팀 승리 플랜 기준" 가중치([ContributionScoreWeightsV4.txt](LolHelperBot/Config/ContributionScoreWeightsV4.txt))로 계산하며, 신규 지표(경험치·라인솔로킬·오브젝트 몬스터종류 보너스·힐+보호막·다른라인기여도/로밍 근사)까지 반영. 새 서비스 `ContributionScoreCalculatorV4`가 계산하고, 결과는 새 테이블 `match_contribution_v4`(guild_id, match_id, discord_user_id, early_score, late_score, final_score)에 미리 저장해서 명령 실행 때마다 재계산하지 않음. **2026-08 매치 49건만 백필**(`dotnet run -- v4-backfill 2026-08`) — 백필 안 된 경기는 자동으로 v3(전체 게임 기준)로 폴백. `/전적수집`은 아직 안 건드려서, 앞으로 새로 수집되는 경기는 v4-backfill을 다시 돌려야 점수가 채워짐(다음 단계 — AfterUpgrade.md 참고).
- `/아재전적` UI: 라인 아이콘 변경(탑 🛡️→🪓, 서폿 ⚜️→🛡️).
- 콘솔 실험 도구(`timeline-raw`, `v4-test` 등) 결과가 한글 로그 리다이렉트 시 깨지던 문제 — `Console.OutputEncoding`을 UTF-8로 고정.

## [0.6.0] - 2026-08-21

### 변경

- `/아재전적` UI 개선: 줄마다 반복하던 🟢 승패 표시를 없애고 날짜 앞에 한 번만(🔵승/🔴패) 표시. 각 줄 라인 이름 앞에 초록 원 대신 라인 아이콘(🛡️탑/🌿정글/🧙미드/🏹원딜/⚜️서폿) 추가.
- 기여도 점수 가중치 미세 조정: 사용자가 캡처한 OP.GG 전적 4경기와 봇 기여도 순위를 대조해본 결과, 원딜이 팀 내 딜량 1위인데 기여도가 낮게 나오거나 서폿이 OP.GG 중위권인데 👑을 받는 사례가 반복돼 `BOTTOM.damage_dealt`(0.6→0.8), `UTILITY.kda`(1.0→0.8), `UTILITY.vision_score`(0.8→0.6) 조정. 표본이 4경기뿐이라 미세 조정 수준 — 맞라인 비교(v3) 설계 자체는 유지.

## [0.5.0] - 2026-08-20

### 변경

- 메타픽 추천에 해당 챔피언의 AZ 자랭 판수·승률·메타 대비 승률 차이와 모든 주 플레이어별 판수·승률을 표시.
- 메타픽 AZ 승률의 신뢰 표본을 3판으로 설정하고 `🐝 검증`, `⚠️ 주의`, `🧪 표본 부족/미플레이`로 구분.

## [0.4.0] - 2026-08-20

### 변경

- `/밴픽추천`을 라인별 임베드로 분리해 전체 라인 조회의 경계를 명확하게 표시.
- 우리팀 추천픽이 op.gg OP~1티어와 겹치면 `🐝`로 표시하고, 승률·픽률·밴률을 함께 평가한 라인별 메타픽 TOP 3를 별도 제공.

## [0.3.0] - 2026-08-20

### 추가
- `/전적등록후보 기준아이디: [최근경기수]` (운영자 전용) — 기준 계정의 최근 자유 랭크 경기에서 자주 같은 팀이었던 미등록 계정을 찾아 등록 후보로 보여줌. 새 멤버 등록 시 롤 아이디를 일일이 물어보지 않아도 되게 하기 위함.
- `/바텀듀오` — 원딜+서폿 **멤버** 조합별 승률을 모스트 10(승률 50% 이상) / 워스트 10(승률 50% 미만)로 보여줌. `/조합추천`(챔피언 궁합)과 달리 사람 궁합.
- `/봇듀오챔프승률` — 원딜+서폿 **챔피언** 조합별 승률 모스트 10 / 워스트 10.
- `/정글미드듀오챔프승률` — 정글+미드 **챔피언** 조합별 승률 모스트 10 / 워스트 10.
- **`/밴픽추천 [라인]`** — 우리 클랜 데이터 기준 밴픽 추천. 베스트 픽(라인 지정 시 TOP5, 생략 시 라인 통합 TOP10, 승률 50% 이상)과, 같은 라인에서 상대했을 때 우리 승률이 50% 미만이었던 **주의 챔피언 TOP5**(💀)를 함께 보여줌. 사용자가 제안한 "PS채널/op.gg류 일반 메타 카운터픽 벤치마킹" 아이디어는 `lol.ps`의 `robots.txt`가 대상 페이지를 명시적으로 크롤링 차단하고 있어 보류하고, 우선 **클랜 자체 실전 데이터만으로** 구현(임베드 하단에도 이 점을 명시). 이를 위해 `match_participations`에 `opponent_champion_name`(같은 라인 상대 챔피언) 컬럼을 신규 추가하고, `/전적수집`·`/리플업로드`·`/부캐충돌해결` 저장 시점에 자동으로 채우도록 함.
- **`/밴픽추천` 2단계(라인별 픽/밴 개편 + op.gg 메타 스냅샷 연동).** 라인 미지정 시에도 5라인 전부를 라인별로 분리해서 보여주도록 개편하고, 각 라인 **픽 3개 / 밴 최대 3개**(조건 충족 시에만, 억지로 채우지 않음) 구조로 변경.
  - 밴 3개는 서로 다른 근거로 각 1개씩: (1) 맞상대 승률 낮은 챔프(기존 로직), (2) 메타 티어 높은 챔프(신규 — 아래 스냅샷), (3) **우리 라인 Top3 베스트픽이 상대했을 때 승률이 유난히 낮았던 상대**(신규 — `MatchRepository.GetMatchupStatsAsync`, "우리 티어픽 카운터"). 같은 챔피언이 중복 추천되지 않도록 앞 기준에서 뽑힌 챔피언은 다음 기준에서 제외.
  - **op.gg 메타 데이터는 자동 크롤링 대신 수동 스냅샷 파일**([Config/MetaTierSnapshot.json](LolHelperBot/Config/MetaTierSnapshot.json), 형식은 [README](LolHelperBot/Config/MetaTierSnapshot.README.md) 참고)로 연동 — op.gg는 `robots.txt`는 허용적이지만 ToS가 불확실해서(2026-08-19 결정, AfterUpgrade.md 참고), 사용자가 자동 스크래핑 대신 수동 갱신 방식을 선택함(2026-08-20). 파일이 비어 있어도 클랜 데이터 기반 추천은 그대로 동작하고, 채워두면 픽 추천 줄에 "메타 카운터"가, 밴 추천에 "메타 티어 높은 챔프" 후보가 추가됨. `MetaTierRepository` 신규 서비스가 매 명령 실행마다 파일을 다시 읽어서 봇 재시작 없이 반영됨.
  - 검증용으로 `dotnet run -- banpick-test [라인]` (Discord 없이 DB 쿼리만 콘솔에 출력) 추가.
- **Match-V5 Timeline API 실험용 코드 추가** (`RiotApiClient.GetTimelineAsync`, `dotnet run -- timeline-test [매치수]`). AfterUpgrade.md의 "15분 라인전/후반 분리" 아이디어를 실제 기능으로 만들기 전에, 최근 클랜 매치 10건으로 데이터 모양부터 확인해본 1단계 실험 — 15분 시점 라인별 골드/XP/CS 차이, 킬 이벤트 분포, 최대 골드 스윙 구간을 콘솔에 출력. 결과 확인 결과 15분 분리가 전체 게임 결과만으로는 안 보이는 신호(라인전은 이겼는데 게임을 짐/그 반대)를 잡아내는 게 확인됨 — 아직 `/함께한전적`·`/명예의전당` 실제 점수 계산에는 반영 안 함(다음 단계).
- **게임별 기여도 순위 + `/명예의전당`(월별 베스트 랭킹) 신규 추가.** 유어지지 화면을 참고한 아이디어를 바탕으로, 딜량/받은 피해량/경감 피해량/골드/시야 점수/CC 시간/킬 관여율을 라인별로 다르게 가중치를 줘서 **그 판에 같이 뛴 우리 팀 AtoZ 5명끼리만** 상대 비교하는 방식(A안)으로 구현. 전체 유저 모수 대비 퍼센타일 방식(B안, op.gg/u.gg 느낌)은 클랜 표본이 아직 작아서 보류 — README "다음 단계"에 남겨둠.
  - `ContributionScoreCalculator` 신규 서비스: 가중치를 코드가 아니라 [ContributionScoreWeights.txt](LolHelperBot/Config/ContributionScoreWeights.txt)에서 읽어와서, 코드 수정 없이 그 파일 숫자만 바꾸면 튜닝되도록 함(사용자 요청 — "점수 수치화 로직을 txt 파일로 볼 수 있게").
  - `match_participations`에 `damage_dealt`/`damage_taken`/`damage_mitigated`/`gold_earned`/`vision_score`/`cc_time_dealt` 6개 컬럼 신규 추가(전부 nullable). `RiotApiClient`가 매치 상세 조회 시 이미 받아오던 응답에서 값만 더 추출하도록 확장 — API 호출은 늘지 않음. `.rofl` 업로드는 이 지표가 없어서 해당 경기는 순위 계산에서 자동 제외됨.
  - `/함께한전적`에 그 판 기여도 순위(👑 1위 / 💀 5위 / `(N위)`)를 각 참가자 줄에 표시.
  - `/명예의전당 [연월]`(전체 멤버 사용 가능) — 지정한 달(기본 이번 달, KST 기준)에 그 판 기여도 1위를 가장 많이 한 멤버 랭킹(1위 1회=100점, 워스트 횟수도 같이 표시). **월별 집계**(사용자 요청 — "월별로 초기화") — 별도 누적 테이블 없이 매번 그 달 데이터로 새로 계산하는 방식이라 가중치를 나중에 바꿔도 과거 달 순위가 자동으로 새 기준을 반영함.
- **가중치 표를 YOUR.GG(유어지지) 벤치마킹 기반으로 재설계 + 실데이터로 3차 검증.** 사용자 요청으로 유어지지를 참고하려 했으나 실제 페이지는 403(봇 차단)으로 못 읽어서, 사용자가 이전에 보여준 스크린샷과 나무위키/인벤 커뮤니티 설명(레이더 차트 5축: 15분 골드차이·생존·시야·성장·전투)을 참고해 라인별 가중치를 다시 설계함. 이 과정에서 `deaths`(생존) 지표를 새로 추가(순위 기반, 데스 적을수록 고득점).
  - 실데이터 검증 1차(팀 내 비율% 정규화): 서폿이 8경기 중 6경기 1위 — 시야점수·킬관여율을 서폿이 구조적으로 독점해서 발생(유어지지 자체도 "서포터/탱커 과대평가" 편향이 있다고 나무위키에 언급됨).
  - 2차(정규화를 "5명 중 순위" 방식으로 전환): 오히려 7/8로 심화. 원인은 라인마다 가중치 총합이 달라서(서폿 3.4 vs 미드 2.3 등) 가중치 합이 큰 라인이 항상 유리했던 것 — 최종 점수를 (가중합 / 가중치 총합)으로 나눠 라인마다 만점을 100점으로 통일하는 정규화를 추가.
  - 3차(30경기 재검증): 이번엔 원딜 63%(19/30) 1위 — 지표 개수가 적은 라인(원딜 4개)이 지표 많은 라인(탑 6개)보다 평균이 덜 흔들려서 계속 유리했던 것. 원딜 지표를 6개로 늘리고 탑/정글 가중치도 재조정했으나, 재검증 결과 여전히 원딜 47%·서폿 33% 대 정글 0%(30전 전패)의 편향이 남음 — 특정 지표를 확실히 독점하는 라인(원딜=딜량, 서폿=시야)이 유리하고, 정글처럼 한 지표도 확실히 독점 못 하는 "제너럴리스트" 라인은 계속 불리한 구조적 한계를 확인. `ContributionScoreWeights.txt`에 이 한계를 명시하고, "참고 지표로 활용, 완벽한 심판으로 맹신 금지"로 문서화함.

### 변경
- 운영자 명령을 `/atoz` 아래로 통합: `/atoz 전적수집`, `/atoz 전적등록후보`, `/atoz 부캐충돌목록`, `/atoz 부캐충돌해결`, `/atoz 리플업로드`.
- `.rofl` 저장분은 내전 전용 큐(0)로 기록해 자유 랭크 큐(440) 통계와 분리.
- 2026-08-20 op.gg 라인별 티어 캡처 5장을 `MetaTierSnapshot.json`에 반영하고 숫자 티어 정렬을 지원.
- **리팩토링 2단계 — `/티어픽` 계산 로직을 서비스 계층으로 분리(2호) + N+1 쿼리 제거.** `ShowChampionTierAsync`의 쿼리·필터·정렬 로직을 새 `ChampionTierService`(순수 데이터 계산)로 이관하고, 커맨드 핸들러는 "서비스 호출 → Embed로 그리기"만 담당하도록 축소. 겸사겸사 외부 코드리뷰에서 지적된 N+1 쿼리(챔피언 한 줄마다 `GetChampionPlayersAsync`를 따로 호출하던 부분)를 `MatchRepository.GetChampionPlayersBatchAsync`/`GetOverallChampionPlayersBatchAsync`(SQLite row-value `IN` 문법으로 필요한 조합을 한 번에 조회)로 교체. `GetPositionOrder`가 Module과 Service 양쪽에서 필요해져서 `Services/PositionOrder.cs` 공용 유틸로 분리. `dotnet run -- tier-test`로 원시 단일 쿼리와 대조해 회귀 확인(일치 확인됨).
- **리팩토링 2단계 — `/밴픽추천` 계산 로직을 서비스 계층으로 분리(시범 1호).** 외부 코드리뷰의 "커맨드 핸들러가 필터링·집계·추천 로직까지 직접 수행한다" 지적에 따라, `ShowBanPickRecommendationAsync`(약 150줄)에 있던 쿼리·후보 선정·중복 제거 로직 전부를 새 `BanPickRecommendationService`(Discord 타입을 전혀 모르는 순수 데이터 계산)로 옮기고, 커맨드 핸들러는 "서비스 호출 → Embed로 그리기"만 담당하도록 축소. 리팩토링 전/후 결과가 같은지 `dotnet run -- banpick-test`에 서비스 결과 출력을 추가해서 원시 쿼리 결과와 나란히 대조 확인(라인별 픽 Top3·밴 3종 전부 동일 출력 확인됨). 앞으로 다른 커맨드(티어픽, 명예의전당 등)를 같은 패턴으로 옮길 때 이 파일을 템플릿으로 참고.
- **리팩토링 1단계 — 상수/유틸 중복 제거 + 실험 코드 위치 정리.** 외부 코드리뷰에서 "매직 넘버 산재"·"중복 코드"·"실험 파일이 정식 진입점에 배선됨" 지적을 받고 정리함(가장 위험한 서비스 계층 추출·인터페이스 DI·테스트 프로젝트 도입은 이번엔 하지 않기로 함 — 서비스 하나 없는 소규모 클랜 봇 규모에서 과할 수 있어 보류, 다음 단계에서 필요성 재검토).
  - `FlexQueueId`(4곳)·`MinSampleSize`(2곳)·`RiotApiDelay`(2곳)가 파일마다 따로 하드코딩돼 있던 걸 `Services/ClanConstants.cs` 하나로 모으고, 각 파일은 `using static`으로 그대로 가져다 씀(호출부 코드는 안 바뀜, 값의 출처만 통일).
  - `TryParseRiotId`(3곳 복붙), `EscapeMarkdown`(2곳 복붙), `CanManageMembers`(2곳 복붙)를 각각 `Services/RiotIdParser.cs`·`MarkdownFormatter.cs`·`PermissionChecker.cs`로 이관(`CanManageMembers`는 `SocketInteractionContext` 확장 메서드로 전환 — 호출부는 `CanManageMembers()` → `Context.CanManageMembers()`로만 변경).
  - `TimelineExperiment.cs`·`BanPickQueryExperiment.cs`를 `Services/`에서 `Tools/`(네임스페이스 `LolHelperBot.Tools`)로 이동 — "실험용"이라 밝혀놓고 정식 진입점(`timeline-test`/`banpick-test`)에 배선돼 있어 프로덕션 코드와 구분이 안 된다는 지적을 반영, 위치와 네임스페이스로 개발 도구임을 명확히 함.
- **`/함께한전적` → `/아재전적`으로 개명 + `최소인원` 파라미터 제거(항상 5인큐만 조회).** `/전적수집`·`/리플업로드`가 이미 같은 팀 5명 전원이 우리 멤버로 확인된 경기만 저장하므로, `최소인원`을 5보다 낮게 줘도 결과는 늘 5명 경기와 같았음(README에 참고사항으로 적혀 있던 내용) — 있으나 마나 한 파라미터라 아예 없애고, "AtoZ 멤버 5명이 5인큐한 경기만 보는 명령"이라는 걸 이름에서부터 명확히 함. `개수` 파라미터(기본 5, 최대 10)는 유지.
- **모스트/워스트 계열 통계 전부 승률 50% 기준으로 일관되게 정리.** `/내전적`(모스트·워스트 챔피언 TOP 3), `/솔랭전적`·`/자랭전적`(모스트 TOP 3), `/바텀듀오`·`/봇듀오챔프승률`·`/정글미드듀오챔프승률`(모스트10·워스트10) 전부 모스트는 승률 50% 이상, 워스트는 50% 미만만 후보로 삼도록 통일. 표본만 채웠다고 애매한 승률이 섞이는 걸 방지. (이전엔 `/내전적` 워스트만 "50% 이하" 기준으로 부분 적용돼 있었음 → 이제 "이상/미만"으로 정확히 반씩 나뉘고 다른 명령에도 동일 규칙 적용)
- `/전적수집`의 `최근경기수` 상한을 50 → **300**으로 확대하고, 100경기를 넘는 요청은 내부적으로 여러 페이지(`start` 이동)로 나눠 조회하도록 변경. 오래된 매치를 놓쳤을 때 큰 값으로 "딥 백필" 가능.
- **최소 표본 기준을 2판 → 6판(5판 이하 제외)으로 강화.** `/티어픽`, `/내전적`, `/조합추천`, `/바텀듀오`, `/봇듀오챔프승률`, `/정글미드듀오챔프승률`, `/솔랭전적`·`/자랭전적`(모스트 TOP 3)에 적용. `/승률순위`에도 동일 기준의 표본 필터를 새로 추가(기존엔 필터 없이 전체 표시).
- **모스트/승률 계열 1·2·3위에 👑/🥈/🥉 메달 표시 추가, 워스트 계열 1위에 💀 표시 추가.** `/내전적`, `/바텀듀오`, `/봇듀오챔프승률`, `/정글미드듀오챔프승률`(모스트=👑🥈🥉/워스트=💀만), `/승률순위`·`/조합추천`·`/솔랭전적`·`/자랭전적`의 모스트(👑🥈🥉, 워스트 없음).
- 승률 동률 시 판수 많은 쪽이 위로 가는 정렬 규칙은 이미 모든 승률 기준 통계에 적용돼 있던 걸 재확인함 (추가 수정 불필요).
- **`/티어픽` 개선**: 각 라인 1위에 👑 표시 추가. 라인별 **TOP5 전부**(1~5위) 그 챔피언을 실제로 플레이한 멤버 이름과 **지분율(%)**을 괄호로 같이 표시(처음엔 1~3위만이었다가 5위까지 확대). 라인 구분 없이 **전체 챔피언 대상 워스트 5개**(승률 50% 미만, 1위 💀)를 별도 필드로 추가, 여기도 플레이어별 지분율 포함.
- **`/내전적` 라인별 승률에 그 라인의 모스트 챔피언 TOP 3 추가.** 각 라인 줄 아래에 "챔프 N판 · 승률X%" 형식으로 그 라인에서 가장 승률 좋았던 챔피언 최대 3개를 표시. 라인 자체 표본이 이미 적은 경우가 많아 전체 모스트/워스트에 적용되는 최소 6판 기준은 적용하지 않고, 판수를 항상 같이 보여줘서 표본 크기를 직접 판단할 수 있게 함.

### 검증 / 수정
- **데이터 정합성 전수 검사 실시** — PK 중복(2번 이상 저장), 팀 인원 불일치, `checked_matches`/`match_participations` 정합성을 직접 쿼리로 확인. 완전한 중복 저장은 없었음(PK 제약 정상).
- **[발견] 부캐 소유자 충돌로 인한 데이터 누락 9건.** 부캐의 기본 소유자와 그 부캐를 실제로 플레이한 다른 사람이 같은 경기·같은 팀에 동시에 나오면, 두 사람이 DB상 같은 discord_user_id로 겹쳐서(`match_participations`의 PK가 `discord_user_id` 포함) 한쪽 기록이 조용히 저장 안 되고 있었음(팀 인원 5명이 아니라 4명으로 저장됨). Riot API는 디스코드 신원을 모르므로 사전 감지가 불가능해 완전한 자동 수정은 어려움.
- **`/전적수집`에 부캐 소유자 충돌 경고 추가.** 이제 이런 경기를 발견하면 결과 메시지에 "🔀 부캐 소유자 충돌 N건"으로 매치ID와 함께 표시됨.
- **부캐 소유자 충돌 매핑 기능 신규 추가.** 충돌난 참가자 정보(매치ID·롤아이디·챔피언·라인·기본 소유자)를 `match_owner_conflicts` 테이블에 저장하고, `/부캐충돌목록`(운영자 전용, 미해결 충돌 목록 조회)과 `/부캐충돌해결 매치아이디: 롤아이디: 멤버:`(운영자 전용, 실제 플레이어를 지정해 그 계정·그 경기 기록만 재저장, 다른 계정 데이터는 안 건드림)로 리플 파일 없이도 Riot API 재조회만으로 바로잡을 수 있게 됨. 충돌 그룹 중 이미 정상 저장된 쪽(PK 경쟁에서 이긴 쪽)은 자동으로 해결됨 처리해서 `/부캐충돌목록`엔 진짜로 누락된 쪽만 표시됨.
- **`/티어픽` 지분율 표시 방식 변경**: 기존 "이름 N%"(챔피언 전체 판수 대비 비율) → **"이름 N판 · 승률X%"**(그 사람의 실제 판수·승률)로 변경. 판수 많은 순 정렬은 그대로 유지.

### 제거
- **`/솔랭전적`, `/자랭전적` 명령어 제거.** 개인 랭크 전적은 op.gg 등 외부 사이트로 충분하다고 판단해 삭제. `RankedHistoryModule.cs` 파일 및 `RiotApiClient.GetRankedMatchHistoryAsync`/`RankedMatchSummary`/`RankedMatchHistoryResult` 관련 코드 전부 정리. 클랜 자체 데이터 기반 통계(`/티어픽`, `/승률순위`, `/내전적` 등)는 영향 없음.

### 수정
- **[버그] 새 멤버 등록 후 `/전적수집`이 그 사람이 낀 경기를 계속 놓치던 문제 수정.** 새 멤버가 등록되기 전에 이미 "5명 미달"로 확인·스킵된 매치는 `checked_matches`에 캐싱돼서, 나중에 그 사람을 등록해도 다시 평가되지 않고 계속 무시됐음(실제로 확인 결과 매치 10건이 이 버그로 누락돼 있었음). 이제 `/atoz 멤버등록`·`/atoz 부캐등록`으로 새 계정이 등록되면 "확인함(저장 안 됨)" 캐시를 자동으로 초기화해서, 다음 `/전적수집`이 그 매치들을 다시 평가하도록 수정.

### 데이터
- 2026-08-19: 두 멤버(기, HoonJP)의 듀오 전적을 API로 직접 검증하다가, `/전적수집`의 `최근경기수` 상한(당시 50)보다 오래된 매치 15건이 5명 조건을 충족하는데도 수집되지 않은 걸 발견 — 위 "상한 확대(300)" 변경으로 `최근경기수:300` 등으로 재수집하면 백필 가능.
- 2026-08-19: `opponent_champion_name` 컬럼 추가에 맞춰, 기존에 저장돼 있던 매치들의 상대 챔피언 정보를 Riot API 재조회로 채우는 **1회성 백필**을 운영 DB에 직접 실행함(기존 행은 전혀 삭제/변경 없이 컬럼만 채움). 1차 실행은 Riot API 요청 속도가 분당 한도를 넘어 133건이 `429 TooManyRequests`로 실패했고(295건 중 162건만 성공), 요청 간격을 늘리고 429 시 자동 재시도하도록 스크립트를 보완해 재실행 — 남은 133건 전부 성공. 최종적으로 매치 295건 전체 조회 성공, 총 1,466행(기존 전체 데이터) 전부 `opponent_champion_name` 채움 완료(빈 값 0개).
- 2026-08-19: 기여도 점수용 `damage_dealt`/`damage_taken`/`damage_mitigated`/`gold_earned`/`vision_score`/`cc_time_dealt` 컬럼 추가에 맞춰 기존 295경기를 Riot API로 재조회해 채우는 **1회성 백필**을 운영 DB에 직접 실행(적용 전 DB 백업 후 진행, 성공 확인 후 백업 삭제). 1차 실행은 저장된 puuid로 매칭했는데, 예전 API 키 시절에 저장된 행은 지금 API가 주는 puuid와 값이 달라 957/1,466행만 채워지고 509행이 비었음(과거 API 키 교체 이슈와 동일 원인, `/내전적` 데이터 검증 때 이미 확인했던 문제) — puuid 대신 `opponent_champion_name` 백필과 같은 방식으로 (팀ID, 라인) 조합으로 매칭하도록 스크립트를 고쳐 재실행 → 남은 509행 전부 성공. 최종적으로 매치 295건 전체 조회 성공, 총 1,466행 전부 채움 완료(빈 값 0개).

> **이 날 나눈 대화에서 그대로 옮겨 적은 원본 메모(비정형, 참고용)** — 위 항목들로 정리되거나
> 보류된 배경 설명입니다. 진행 상황이 정리된 버전은 [AfterUpgrade.md](AfterUpgrade.md) 하단
> "사용자 원본 메모"에서 계속 관리합니다.
>
> - [메모리트리] 보통 내 기준으로는 상대법이나 상성은 피에스채널이 정확하고
> - [메모리트리] 지표는 유어지지가 보기 좋고 > 유어지지에서 첨삭해서 우리 지표로 가져와보자(훈레사)
> - [메모리트리] 전적검색은 오피지지가 좋더라
> - 1. 라인별 추가 점수 지표 넣어서 게임별 12345위 지표 또는 게임당 베스트 플레이어, 워스트 플레이어 정하기, 베스트플레이어 점수 100점씩해서 명예의 전당 기능 //훈레사
> - 2. 골드차이 팍내는 구간 데이터에 누가 기여를 많이했는가 //훈레사
> - 6. 내전적 치면 나는 뭐 딜러형서포터, 뭐 그런 수식어 넣을수있는 데이터 있는지(탑 탱커 브루저 뭐 이런거 미드도 마찬가지 그런느낌으로 데이터 분류도 되는지 봐야겠다)
> - 7. 이번달 참여왕 승률왕
> - 8. 내전 지표는 이제 따로 데이터 관리해서 내전티어표랑 내전은 리플레이로 데이터 저장하게

## [0.2.0] - 2026-08-19

### 추가
- **부캐 등록**: `/atoz 부캐등록` — 본캐에 다른 Riot 계정을 연결. 부캐로 플레이한 경기도 전적 통계에서 본캐 기준으로 합산됩니다.
- **멤버 관리(운영자 전용)**: `/atoz 멤버목록`(등록 현황 확인 — 실명/Riot ID/수집된 경기 수), `/atoz 멤버삭제`, `/atoz 부캐삭제`.
- **클랜 전적 통계** — 등록 멤버들의 자유 랭크(5vs5 Flex, queue 440) 전적을 모아 집계하는 기능 일체를 새로 추가:
  - `/전적수집` (운영자 전용) — Riot API로 매치 데이터를 배치 수집해 SQLite에 저장.
  - `/티어픽 [라인]` — 라인별 챔피언 승률 TOP 5.
  - `/승률순위` — 등록 멤버 승률·KDA 순위.
  - `/내전적 [멤버]` — 개인(또는 지정 멤버)의 총 승률·라인별 승률·모스트/워스트 챔피언 TOP 3.
  - `/조합추천` — 같은 팀에서 함께 나왔을 때 승률 좋은 챔피언 조합 TOP 10.
  - `/함께한전적 [최소인원] [개수]` — 같은 팀에 AtoZ 멤버가 N명 이상 있었던 경기 목록.
- **리플(.rofl) 업로드**: `/리플업로드` — 리플레이 파일 메타데이터를 직접 파싱해 전적 데이터를 미리보기/저장. 여러 명이 돌려쓰는 부캐 계정을 위한 `재지정` 옵션 포함. 파일명(`지역-게임ID.rofl`)에서 실제 Riot 매치ID를 추정해 `/전적수집` 데이터와 겹치지 않도록 시도.
- **기능 안내**: `/기능안내` — 일반 멤버가 쓸 수 있는 조회 명령어 가이드 (ephemeral).
- AtoZ 클랜 디스코드 봇 아이콘 제작 ([assets/atoz-bot-icon.png](assets/atoz-bot-icon.png)) — 원소술사 럭스 테마 오마주 + "AZ" 모노그램.
- `CHANGELOG.md`(이 파일) 신설.

### 변경
- `/솔랭전적`, `/자랭전적`: 조회 경기 수를 10 → 30경기로 확장. 요약(승률)·모스트 TOP 3는 최근 30경기 기준으로 집계하고, 경기별 상세 기록은 그중 최근 10경기만 표시.
- `/조합추천`: 멤버 듀오 승률 기준 → **챔피언 조합** 승률 기준으로 변경 (누가 픽했는지와 무관하게 챔피언 궁합만 집계).
- `/전적수집`, `/리플업로드`: **같은 팀 5명 전원이 AtoZ 멤버(본캐/부캐)로 확인된 경기만** 저장하도록 변경 — 매치메이킹으로 낯선 사람과 섞인 경기는 통계에서 제외됩니다. 저장 대상이 아니었던 매치도 `checked_matches` 테이블에 "확인 완료"로 기록해 다음 수집 때 Riot API를 다시 호출하지 않도록 최적화.

### 데이터
- 2026-08-19: 위 "5명 전원 필터" 도입에 맞춰 기존에 쌓인 `match_participations`(매치 통계) 데이터를 초기화했습니다. `members`/`member_alt_accounts`(등록 정보)는 그대로 유지.

## [0.1.0] - 2026-08-18

### 추가
- 프로젝트 최초 스모크 테스트 구현.
- `/ping` — 디스코드 연결 확인.
- `/riotcheck` — Riot API 연결 확인.
- `/atoz 멤버등록` (운영자 전용) — Discord 사용자 ↔ Riot 계정 연결.
- `/솔랭전적`, `/자랭전적` — 최근 10경기 전적 조회 (승패/챔피언/K·D·A/포지션/CS/경기 시간 + 모스트 TOP 3).
